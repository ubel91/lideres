<form method="post" action="{{ path('libro_activado_delete', {'id': libro_activado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ libro_activado.id) }}">
    <button class="btn">Delete</button>
</form>
