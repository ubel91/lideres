<form method="post" action="{{ path('imagen_guardada_delete', {'id': imagen_guardada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imagen_guardada.id) }}">
    <button class="btn">Delete</button>
</form>
