{% extends 'base.html.twig' %}

{% block title %}TipoRecurso{% endblock %}

{% block body %}
    <h1>TipoRecurso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_recurso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_recurso.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_recurso_index') }}">back to list</a>

    <a href="{{ path('tipo_recurso_edit', {'id': tipo_recurso.id}) }}">edit</a>

    {{ include('tipo_recurso/_delete_form.html.twig') }}
{% endblock %}
